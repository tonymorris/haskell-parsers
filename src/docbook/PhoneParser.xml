<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
    "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">

<sect1 id="PhoneParser" xmlns:xi="http://www.w3.org/2001/XInclude">
  <title>phoneParser</title>
  <para>
    We will use the <code>phoneBodyParser</code> and the <code>is</code> parser to create a parser for the phone number.
    Once again, we glue it all together using <code>bindParser</code> and <code>mapParser</code>. Notice that we ignore
    the hash character in the end when we produce the final string.
    <programlisting lang="Haskell"><xi:include href="21/MyParser.hs" parse="text"/></programlisting>
    <emphasis>Phone: string of digits, dots or hyphens but must start with a digit and end with a hash (#)</emphasis>
  </para>
</sect1>
